## Linux Sources List
```sh
# 下载新的 Debian 13 Trixie 源列表（需确保已安装 wget，或使用 PowerShell 时的 wget/Invoke-WebRequest）
wget "https://raw.githubusercontent.com/CodiFelix/apt-sources-list/main/debian13trixie" -O debian13trixie.txt
```
